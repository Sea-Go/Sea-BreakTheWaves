# Workflow Runtime

This directory owns workflow runtime state and shared stage metadata.

Current contents include:

- Workflow stage constants
- Runtime session state
- Skill result contract
- Deterministic planning decision metadata
