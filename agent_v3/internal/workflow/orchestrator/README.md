# Workflow Orchestrator

This directory owns the behavior-compatible workflow orchestrator.

Current responsibilities:

- Requirement intake and merge prompts
- Runtime lifecycle and in-memory session store
- Deterministic readiness decisions
- Stage routing before graph execution begins
