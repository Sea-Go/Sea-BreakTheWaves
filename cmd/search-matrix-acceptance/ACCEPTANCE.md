# H10.b 搜索十二格实际检索执行验收

日期：2026-09-15。状态：**本地锁定 BGE-M3 + Go exact 三路 + tRPC-Agent-Go Search Graph 通过；十二格交付端尚未通过**。这是默认关闭的合成夹具，不是线上效果验收。

`training/search/matrix_source.py`从独立、严格的 H10.b Parquet reader 按调用方给定 SHA 重读 `revision=2` 的 test split，导出 2 个原始查询、查询时间、family/near-duplicate 身份和 4 条分级判断。导出不宣称候选池判断完整，也不补造真实用户身份。Go 入口依次复核锁定 BGE-M3 表示 manifest、模型/profile SHA、Python 三路原始评分和导出文件 SHA；`RunExactWith` 构建现有 Dense/Sparse/Multi-vector exact 索引，回调期间将**同一组活索引及其真实 ref**接入 `search.Service`。每个查询只允许 scorer 固定的时间可用候选修订；三路搜索在每次 Graph 执行内真实运行，非排序结果回放。

本机固定输入：H10.b manifest SHA `10ae33b8dd5b5fc634493a5eddfdbf47105c16c7c9ced57279eee96c13b1bdb8`；BGE-M3 表示 SHA `719b4994b37133a8c6a82e60b333f3d015c4a8d1d9f965f7a2cb7c0a226a68d9`；独立 Python test scorer SHA `200d3e2c4f9229dc85efa33e4ebbcc2b0379975f99b8e15482ef8888c3703355`。导出 SHA `24910a98923518ec5f8b0e82ca9252dc95c7f825ecc2265981c9c058b119ddc9`，由上述 qrel manifest 的本地不可变 Parquet 副本生成。报告为 `/private/tmp/sea-bge-qrel-three-lane.ZMGToD/matrix-execution-r3.json`，文件 SHA `5c59d56c2ebe2e2fd023698e4a3eda12b1fbd6c6f5a38a68da5e0f4f9655f119`，含逐格逐案候选次序、实际 batch/三路耗时、独立 trace ID/框架原生三层 span、成本字段和正式 `EvaluateSearchMatrix` 原报告。

执行结果：`fast/detailed × low/medium/high × summary/tools` 的**检索 Graph**实际调用 12×2=24 次，每次 Graph 和 Runner 都各完成一次，24 个 trace 均有 `invoke_agent search_execute → workflow execute_graph search_execute → workflow execute_function_node execute_search` 框架原生 span。不同档位的 `TopKPerLane/MaxEvidence` 实际生效：fast 的 low/medium/high 每案分别有 1/2/3 条候选，detailed 分别有 2/3/4 条；详细搜索因证据上限停止，`retrieval_status=partial, stop_reason=evidence_limit`。每案一条原始冻结 query，没有合法后续改写，故 detailed 没有多轮查询。另在**本次同一入口**执行 Python↔Go exact 数值对照，6 chunks 的 103 条有效 token rows，三路各比较 12 个评分，Dense 最大偏差 `9.99e-16`、Sparse `0`、Multi-vector `4.44e-16`，三路 TopK 一致，Sparse 的零相交候选按倒排索引合同排除。两次独立调用的正式矩阵 `input_hash=05063d46dd56fc28c9131bd36dbd7f3b111dc21f7ad0c6c264b304066544a6f8` 稳定。

`EvaluateSearchMatrix`收到的是 12 格真实 Graph 候选，且所有 case 均记 `partial`，因为其指定的 summary/tools **交付未执行**。H10.b 无全候选判断完整收据，12 格的 `S09.recall_at_k`、`S11.mrr_at_k`、`S12.ndcg_at_k` 全部 `not_evaluable`，没有零分或提升结论。每格 `model_token_cost=null`、`citation_write_count=null` 表示没有真实 DC 模型会话和 RTW 引用持久化观测；不能把这些字段写成零成本。合成 `SubjectRef` 仅为 `sea.synthetic.qrel/fixture/<query_id>`，与实际 RTW/WhaleHall/DataCenter 账户没有映射。此次没有重启 SeaweedFS S3，使用固定哈希的本地 Parquet 副本；没有真实 HTTP、RTW 同修订正文读取、引用收据、summary LLM 或 ToolSession，所以不是十二条端到端产品路径。

复现时先用 `PYTHONPATH=training uv run --project training --locked --python 3.12 python training/search/matrix_source.py --qrel-manifest-uri <本地或已连接S3的冻结manifest> --qrel-sha256 <H10.b manifest SHA> --split test --output <新文件>`；再执行 `go run -mod=readonly ./cmd/search-matrix-acceptance --representation-manifest <锁定BGE表示manifest> --scorer <独立Python test scorer> --scorer-sha256 <其字节SHA> --case-export <上步新文件> --case-export-sha256 <其字节SHA> --artifact-dir <独立临时对象目录> --output <新报告>`。两个输出都须是新路径。`go test -mod=readonly -race -count=1 ./cmd/search-matrix-acceptance ./internal/retrieval/three_lane_parity ./internal/search ./internal/evaluation`、对应 `go vet`、Python `py_compile` 与 `git diff --check` 均退出 0。

后续交接：RTW 提供同一发布版本的正文读取/可撤回状态/持久引用收据与签名主体；DataCenter 提供明确模型配置及实测模型调用/Token 用量；搜索产品 owner 给六档各自的真实 planner 策略与后续查询预算。用这三方同一次 fixture 服务启动下的 12 条 summary/tools HTTP 请求替换 `delivery_execution=not_executed`，补独立成本和完整判断范围收据后才可讨论十二格正式效果。
