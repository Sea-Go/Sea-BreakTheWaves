# Wiki编制Worker入口默认关闭与装配门禁

状态：**入口可编译但正式Wiki job默认拒启动**。RTW/DC提供方已固定`job_type=content.wiki-compile.v1`、`resource_profile=cpu`和十二键技术票据；应用Interface与本机验证见BTW`internal/app/wiki_compile_worker_acceptance.md`。本入口不重用Prepare/Index的本地对象目录、DC jobs service token或模型路由，也不把候选Wiki当人工发布的Release。

`loadConfig`对`BTW_JOB_TYPE=content.wiki-compile.v1`单独解析`BTW_WIKI_COMPILE_ENABLED`：省略/false时无需任何凭据便保留JobType并转专属`serveWikiCompile`，随后以`content.wiki_compile.start_rejected/SIGNED_DEPENDENCIES_MISSING`结构JSON在Telemetry、PG、DC Claim和模型前退出；不会落入content.prepare.v1的else。显式true仅开放**配置校验**，不是授权运行：`BTW_MODE=local`、`cpu`、服务固定SHA与回环metrics、RTW/DC技术URL及令牌、模型CallPoint=`knowledge-wiki-compiler`、声明的native session来源=`dc-native-app-user`和provider签名的ResultRef合同ID要齐。对象后端为`shared-local`时BTW/RTW同一任务专属绝对root；为`s3`时两个配置bucket要一致，且注入的对象Store须自己实现`Bucket() string`并报告**同一RTW bucket**，Local对象即使并列字符串同名也不能通过S3门禁。这个只读Bucket方法不暴露端点/凭据；同endpoint/权限最终仍由RTW实际ObjectStore读候选原字节/Accept签收。

`wikiCompileStartDeps`实际应由拥有各能力的入口注入DC jobs客户端、RTW GetCompile/GetRevision/ClaimCompile/AcceptCompile、与同RTW桶的`artifacts.Store`、**同一个**`WikiCompileRunFactory`和`CurrentNativeSession` provider。入口只接受配置和deps均为`app.WikiCompileResultRefContractID=sea.wiki.compile-result.v1`，然后在权威装配点直接绑定`CompletionRef=app.BuildWikiCompileResultRef`；调用者不能塞任意fixture callback或仅填一个好看的合同字符串借道DC Claim。入口只从这个模型工厂获取一次DC native app_user账号和`wh_access_`规范令牌，重构为私有typed`WikiCompileModelSession`并核TokenDigest，作为**构造参数**给应用/`RunFactory.Open`；后者返回的AccountID+TokenDigest Proof在Runner模型HTTP**前后**必须相同。jobs service token、RTW JWT、另一个合法账号B或错误账号形状都不能借不同Factory越权写模型用量。A/B错绑在当前应用流程的RTW Claim后拦，模型HTTP/对象Put/RTW Accept/DC Complete仍为零；不虚报DC Claim也为零。若Factory先自报A再在Run后变B，后续对象/RTW/DC写仍阻，但已发生模型调用不能撤销，正式Factory必须直接用冻结A bearer造官方Adapter。令牌形状不是活跃用户证明，DC Gateway仍每次验证native Session/app_user；同逻辑Compile跨租约重试应保持同一DC app_user身份。官方`app.NewWikiCompileNativeRunFactory`已从冻结`session.NativeBearer()`构造`knowledge-wiki-compiler` CallPoint Model，按RTW有序Source RevisionID/ContentHash算JCS指纹、每逻辑Compile同一幂等Ref，最多1024输出/无Tool/非流式；本入口现在要求同一Factory同时实现`CurrentNativeSession/BindTelemetry/Close`。启动门禁冻结一次会话，Bundle `InstallGlobals`后/NewWorker前**唯一**`BindTelemetry(bundle)`，错误记`content.wiki_compile.factory_bind.finished/NATIVE_FACTORY_BIND_FAILED`及`start_failed`，零DC Claim/模型请求。Factory已交付与入口seam可验，并不等于产品背景native app_user账号、有效CallPoint配置或Provider发布已装配；主程序仍零Deps早拒，不能把synthetic fixture自报Proof当真实DC授权。

本入口所注入的DC Jobs客户端现须同时有官方`AcknowledgeCancellation`：RTW Sender在旧Compile被同页新代替后向DC发送Cancel，若旧Job在运行只形成`cancel_requested`/新CV；**原Worker在当前Job栅栏读到同worker/attempt/epoch、CV原值+1且旧租约有效时发ACK请求**，或DC在租约到期的`Get/Claim`懒转`cancelled`。RTW Sender不替Worker发终态ACK。模型迟归后再读DC而看到请求时，本应用先请求确认技术Job取消终态，绝不把旧候选Put/RTW Accept或`CompleteJob(succeeded)`；RTW已`SUPERSEDED`而DC旧Job仍`running`时只Fence等取消，不臆造ACK。取消ACK丢回执需GetJob核同Job/attempt/epoch/CV的`cancelled`，未达终态记partial；DC ACK接口对租约过期已自动`cancelled`的同lease也回200，当前没有施作ACK的独立收据，因此OBS仅`DC_CANCELLED_CONFIRMED`/`dc_cancel_terminal=true`确证技术终态，`rtw_accepted=false`另记领域未接纳。

`serveWikiCompileWithDeps`只在所有能力注入与配置一致后建OBS Bundle，安装tRPC官方Globals、绑定Factory、装Worker并启动metrics/poll；这是测试可用的唯一权威装配seam。poll同步返回后按Go defer实际顺序停metrics→`Factory.Close`等待Runner/Graph节点/候选Sink→`Bundle.Close`冲刷原生Span/指标→最终`content.wiki_compile.stopped`结构JSON。`Factory.Close`错误加入返回`resultErr`并记`factory_close.finished/NATIVE_FACTORY_CLOSE_FAILED`，不会伪报stopped成功；Telemetry初始化或门禁早拒也关闭已注入Factory。未绑定、双绑定、关闭后Open由官方Factory拒，在入口Bind失败处**先于DC Claim**阻断。当前主程序调用`serveWikiCompile`传**零真实依赖**，因此即使操作方把`BTW_WIKI_COMPILE_ENABLED=true`和伪造合同名写进环境，也只能拒启。成功技术ResultRef的v1合同已固定为真实Store读回的13键JCS Manifest，`URI=sha256:<manifestSHA>`、专属MediaType，RTW opaque Accept ResultHash、Wiki MarkdownSHA、DC Job.InputHash和DC Jobs.ResultHash各有不同摘要域。Worker在RTW Accept之后、Manifest前及DC Complete前都GetJob/GetCompile/GetRevision核同一job/attempt/epoch/CV、Wiki+Source未撤回和原文/引用，Manifest由Builder Put/Get后Worker再按同一JCS字节读取；DC409/撤回/取消只留不可变对象与partial，不发成功ACK。RTW人工编辑若先于Accept旧base使业务事务拒；接受后人工继续推进head仍允许原已接纳Job技术完成且不覆盖新head/已发布Release。**主程序当前仍传零真实deps**，所以即使操作者写正确合同ID也在任何Claim/模型/对象副作用前拒；正式native app_user模型账号、S3同桶权限和生产启用尚未装配。

本启动切片`TestWikiCompileCommand*`/`TestWikiCompileNativeStart*`与原LoadConfig/BuildJob/Poll配置聚焦race退出0，日志SHA=`a15fdba6ebe748d20a5891415e43ad23d361ba33936caff75adcbb588d3c0bfb`；app/runtime NativeFactory/CallPoint/Worker/Builder聚焦race SHA=`f29c6f99af95e612ca7dc6c6c63816bf223409665540fc13fb456ee74fdbdb8f`，cmd/app/runtime vet、mod verify、diff check均0。测试确认default-off结构拒启、错误DC jobs token/native UUID/ResultRef/RTW对象root阻断且jobs Claim计数0；S3模式Local Store不能凭bucket字符串越界，只有报告`Bucket()`的fixture adapter可证明**构造参数**匹配。这个fixture不签真实RTW S3 endpoint/ACL或DC active native Session。真实Factory本机test-only以synthetic native会话、不同jobs token、官方HTTP CallPoint fixture跑Graph/Runner完整EOF、Markdown与13键JCS Manifest同Local第二Reader，OTLP Collector收到同Trace原生`tRPC-Agent-Go` Span；先前应用/结果Builder签收日志SHA=`44e3410a91f2d9397613e00d5fc24248234693d713baf331f807852bdd9553f4`；RTW×DC隔离PG16 Holder同父成功L3及停止证据见应用验收页，测试模型只是固定Graph fixture。实际生产装配/在线水位、RTW源EventSpec私有回读、DC native模型账号/有效Provider与正式共享S3、Web人工发布及47任务继续`PARTIAL`。

## 双Holder原生模型本机验收

仅test-only `TestWikiCompileNativeExternalTwoHolder`在环境同时给出`SEA_WIKI_EXTERNAL_RUNTIME_FILE`（RTW Jobs Holder）与`SEA_WIKI_NATIVE_DC_RUNTIME_FILE`（DC Model Gateway Holder）的**两个绝对0600普通文件**时运行；省略即明确SKIP。两份私有JSON分别为`sea.wiki.external-consumer.v1`和`sea.dc.local-chat-consumer.v1`，各十一键，严格拒重复、未知、多尾、非字符串、相对路径、符号链接和宽权限。RTW运行文件的`dc_base_url`是技术Jobs服务，DC运行文件的`endpoint`是另一个模型Gateway且自带`/v1`，Factory只取其去尾`/v1`后的loopback root；模型`user_id/access_token`冻结为同一个DC native app_user会话，不能拿DC Jobs token或RTW Worker token当模型Bearer。测试运行失败时两个Holder release都由task-own cleanup写出，防止PG和进程悬挂；真实模型 JSON/段落引用若被服务器验证拒绝，会保持红而不造固定候选。

首轮固定`ba3aec0`真实Provider请求、原生Graph EOF、RTW `ACCEPTED`及DC `succeeded`均已经发生，但消费者额外要求Markdown首字符`#`，与`ParseWikiCompileCandidate`/RTW Accept内容合同不符，顶层exit1日志SHA=`45e3e8916700d9e9df1b39676f4e49beb2710b062c28117fbbc1b15d6a3fc94c`；DC真实usage是一笔`completed/HTTP200`、Provider `302+419=721` tokens。`41e1270`只移除该不合法test断言，保留正文SHA、标题/来源、RTW Actor/撤回、Manifest、账户/用量/Span核。第二轮**固定远端41e Consumer顶层exit0**日志SHA=`7b1ef217f02632e7de22220436c19081ab5d15f1dcbfdbea35e5e29a5db0f818`：RTW受保护`wiki-holder-result.json`（文件SHA=`29d0bf6a3079d6b63164bd36799f83eb5eb28d2888347a990304958a1962307c`）记录`fixed_model_fixture=false,dc_model_gateway_reported=true,dc_state=succeeded,dc_completed_attempts=1,rtw_edit_head=wiki_revision_id,published_releases=0,production_verified=false`；同RTW对象根的十三键JCS Manifest SHA=`6a5ac6ef1c867ea4fa8cddbdc9413cc34fa9d3be12e9a4121f16b66808fb014d`，已接纳原Markdown SHA=`146f522cc16f69581c5c3e3cd21e594f8435da6e79cdf104a25ca5d3dc93367d`，候选质量引用JSON SHA=`411567d55c16ac43480f73615dad4b9f57518b481adf71f4dddf719e49d660d6`，受限模型proof SHA=`1335537f3e9805fd44e44288f3e7429d73a748ef0a18b1dc000eeeba0c23c23d`，均task-own0600，正文/引用供事实质量评测而不会回显Bearer。DC模型Holder用量文件SHA=`15be8aa17a04b91925cc3cb602efb683998ad253f6dd93a684880ba3987a04c9`，唯一交互`completed/HTTP200`，实际Ollama `qwen3:1.7b` Provider `297+341=638` tokens。RTW nkp7LX父`TestWikiCompilePrivateHTTPAcceptReadsSharedOriginalAndMovesEditHead` PASS85.91s，test.log SHA=`69a1c8ee69d4ddb7a3538a4cf5918e1e4e41ff59a01c7f8910d72436c108de31`；DC ZEyfVL父`TestWS05AModelDraftProbeActivateGatewayRollback` PASS233.02s，go-test.log SHA=`b983c7a5f04ed7bb48a3926a2e476cb299a90ab39b30c5cdd785b07eef7b5cb5`，两份隔离PG16 stop.log SHA=`ca19178a35ab4153b75b494963b66ce8243e1b94c173107c87b44d09db23652d`且记录server stopped；源脚本顶层退出由owner最终对账，受控本机L4不等于生产验证。

`f6e1252`随后仅补test-only私有路径边界，令runtime与usage收据都经`filepath.IsAbs`/`os.Lstat`核真实0600普通文件，既有相对路径和符号链接反例聚焦race通过，日志SHA=`edaa47f6ee256e996b45deba2b52644913a90dad1f3756cf4d4c5d4909aea595`；成功运行的两个Holder确为绝对普通0600文件，业务逻辑未随此补丁改变。本轮质量样本只有一份Synthetic Source正文`one fixed fact`，真模型已接纳Markdown原字节亦是14字节`one fixed fact`、Title=`shared-wiki-page`且有一条`paragraph:1`引用；它证明原事实/引用链，不签可维护AI Wiki页面的结构或表达质量。若产品要强制标题/章节，应先升版候选合同并在入库前验证，不能只在后验test断言首字节`#`。**正式进程仍零deps默认拒启**；本机发布的临时DC native账号/CallPoint和Ollama模型、共享Local对象及受控测试用量不等于后台产品身份、生产Provider配置、同桶S3权限或线上水位。生产启动与Wiki事实质量/人工发布另签，47任务仍`PARTIAL`。
