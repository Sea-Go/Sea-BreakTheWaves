# Wiki编制Worker入口默认关闭与装配门禁

状态：**入口可编译但正式Wiki job默认拒启动**。RTW/DC提供方已固定`job_type=content.wiki-compile.v1`、`resource_profile=cpu`和十二键技术票据；应用Interface与本机验证见BTW`internal/app/wiki_compile_worker_acceptance.md`。本入口不重用Prepare/Index的本地对象目录、DC jobs service token或模型路由，也不把候选Wiki当人工发布的Release。

`loadConfig`对`BTW_JOB_TYPE=content.wiki-compile.v1`单独解析`BTW_WIKI_COMPILE_ENABLED`：省略/false时无需任何凭据便保留JobType并转专属`serveWikiCompile`，随后以`content.wiki_compile.start_rejected/SIGNED_DEPENDENCIES_MISSING`结构JSON在Telemetry、PG、DC Claim和模型前退出；不会落入content.prepare.v1的else。显式true仅开放**配置校验**，不是授权运行：`BTW_MODE=local`、`cpu`、服务固定SHA与回环metrics、RTW/DC技术URL及令牌、模型CallPoint=`knowledge-wiki-compiler`、声明的native session来源=`dc-native-app-user`和provider签名的ResultRef合同ID要齐。对象后端为`shared-local`时BTW/RTW同一任务专属绝对root；为`s3`时两个配置bucket要一致，且注入的对象Store须自己实现`Bucket() string`并报告**同一RTW bucket**，Local对象即使并列字符串同名也不能通过S3门禁。这个只读Bucket方法不暴露端点/凭据；同endpoint/权限最终仍由RTW实际ObjectStore读候选原字节/Accept签收。

`wikiCompileStartDeps`实际应由拥有各能力的入口装配：当前DC jobs客户端、RTW GetCompile/GetRevision/ClaimCompile/AcceptCompile、与同RTW桶的`artifacts.Store`、**同一个**`WikiCompileRunFactory`和`CurrentNativeSession` provider、以及已版本化的`WikiCompileCompletionRef`。入口先从模型工厂拿DC native app_user `AccountID+wh_access_`规范会话令牌；jobs service token、RTW JWT或错误账号形状一律拒。令牌形状不是活跃用户证明，DC Gateway仍每次验证native Session/app_user；同逻辑Compile跨租约重试应保持同一DC app_user身份。模型工厂后续使用单独的BTW`internal/runtime.NewWikiCompileCallPointModel`，按已核RTW源RevisionID/ContentHash顺序算JCS指纹、每逻辑Compile同一幂等Ref，最多1024输出/无Tool/非流式；当前本入口仍未接真实DC有效配置、Provider模型或native服务账号。

`serveWikiCompileWithDeps`只在所有能力注入与配置一致后创建OBS bundle、原生Graph Runtime工厂/worker、metrics及poll；这是一条测试可用的装配seam。当前主程序调用`serveWikiCompile`传**零真实依赖**，因此即使操作方把`BTW_WIKI_COMPILE_ENABLED=true`和伪造合同名写进环境，也只能拒启。RTW/DC owner尚未冻结成功`jobs.ResultRef{URI,Hash,MediaType}`的跨仓映射；任何`fixture://`或把RTW `ResultHash`误作Markdown内容SHA都不能供本入口。正式ResultRef未交付前，本入口不领取真实job、不请求DC模型、不完成DC技术ACK。补齐协议和受控账号后应在这一唯一入口接S3/model/ResultRef正式adapter，再同RTW对象后端与DC job/Model Gateway实际运行验签；不通过API/Prompt改变业务归属。

本机`TestWikiCompileCommand*`与原Prepare/Index/Fact配置回归聚焦race退出0，基础兼容日志SHA=`f311906d0a60d2df4f4ef861ac2eacd216e11750d9573ac7e68eaa9e6d26f404`；新增**所有Wiki命令门禁**和配置通过后Telemetry初始化失败仍结构JSON终态、零DC Claim的定向race日志SHA=`5dcd681a2151c9db08fddbe1d26cbaa7fbefd57cd5ea0f8d272f448b2bac65a9`，cmd vet退出0。测试确认default-off结构拒启、错误DC jobs token/native UUID/ResultRef/RTW对象root阻断且jobs Claim计数0；S3模式Local Store不能凭bucket字符串越界，只有报告`Bucket()`的fixture adapter可证明**构造参数**匹配。这个fixture不签真实RTW S3 endpoint/ACL或DC active native Session。提供方真RTW×DC隔离票据可选只读重播及应用Runner L1日志SHA=`58fa9618ae4a84330862aea8dd04eaa09dcea0724950ac41b3f1db14fb7fab35`、票据SHA=`04d33b7f5e242b80c791c3697bcc8d5a8ac527ff130254607c16222872481aec`，不是同父真实Wiki对象/模型/ACK验收。实际生产装配/在线水位、RTW源EventSpec私有回读、模型授权账号与ResultRef、Web人工发布及47任务继续`PARTIAL`。
