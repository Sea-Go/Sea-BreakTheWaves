# WS09-B 搜索训练首个 CPU 闭环验收（2026-09-14）

状态：`LOCAL_VERIFIED`。输入是代码内生成的 3 train / 1 validation / 1 test 条**合成、显式 qrel 正负对**；没有获准真实搜索 qrels、成熟行为标签、正式 H10.b 搜索数据集或线上效果证据，不能提升为 H11/H12 整链验收。

源码：独立 `feat/search-training-baseline-20260914` 分支的 `training/search/`。环境：Python 3.12.13；根 `training/uv.lock` SHA-256 `95172b2456706711be6a0cc82a0163718e2b8fd660fa069f61e7a5cf9251c485`。冻结输入 fixture manifest SHA-256 `9a1ea37284e17ebed04e92a4d1d67cf855ade3d4000a73f0b807c271733a4e39`（由 `tests/test_training.py::_fixture` 固定生成，临时目录退出后回收）。

验证命令：

```sh
PYTHONPATH=training/search uv run --project training --locked python -m pytest -q training/tests training/search/tests
```

结果：`50 passed in 4.80s`，其中搜索训练专属 11 项。覆盖冻结 manifest/所有文件 hash 和行数、缺片/篡改、错 split 与 query/rewrite-family 跨切分、未判负例/错 grade/mask/来源、train-only IDF、不同配置/数据集/损坏 checkpoint 拒收。中断在累计第 4 次更新后恢复，与不中断训练的最终 checkpoint 和候选工件**字节一致**；随机状态、Adam 状态和 reader 游标均已保存，候选 `active:false`。

实际 CLI 使用相同 fixture、`epochs=8, seed=31` 完成 24 次更新：train 初始 loss `0.6931471805599453`，最终 `0.05312376087891699`；validation 和 test 各 1 对，loss 均为 `0.05387941613979941`，pair accuracy 均为 `1.0`。这组数据由人为构造的简单词面正负对组成，**不能解读为真实相关性或泛化收益**，亦无与当前线上固定模型的公平对照。

交接：WS09-A/WS07-D/E 需要冻结正式搜索 Parquet/schema、qrels revision 和判断覆盖、query/rewrite-family 与近重复内容分组切分、内容时点/generation、明确负例资格与采样引用、SQL 水位和所有文件 hash；训练 reader 的大分片消费与 H10.b 接纳必须独立联验。WS09-D 需完成候选工件推理对照与 DC 加载，WS09-E 需用真实 qrels/固定基准/三路索引、切片和消融验效果。Dense/Sparse/Multi-vector 本轮仅独立声明实验合同，未训练、未导出新索引，见 `experiment-plan.v0.json`。
