# H10.b 搜索QREL到WS09-B词法候选验收

日期：2026-09-15。状态：**合成judged qrel经真实隔离ClickHouse/dbt→SeaweedFS S3→独立Parquet reader→CPU T1候选的L2子链 INTEGRATED；WS09-B/H11/H12整体PARTIAL。** 源代码输入为已推BTW集成`ab5dd37`，本切片只改`training/search/`。仓库`warehouse/search/`与`training/src/sea_training/search_dataset.py`/权威JSON Schema均只读；生产代码不接受训练器自己拼造的manifest或绕过预期SHA。

`search_training.authoritative.open_authoritative_pairs`先调用独立`sea_training.search_dataset.open_search_dataset(manifest_uri,expected_manifest_sha256)`，由其复制并核全部固定Parquet分片的大小/SHA、Arrow列/nullable/行数、来源、查询与内容时点、修订、query family/查询近重复簇跨split和判断mask。训练器仅在已验行上按split内同一query组成grade 2/3高于grade 0/1的显式已判断pair；原0/1/2/3、`judgment_id/revision/source_ref/hash`、query group与mask留在候选字段，等级1不冒名等级0。pair顺序与ID确定性，`sample_weight=grade_gap/3`。各split必须至少一对；20万pair/split上限之外失败，不隐式降采样。pair policy、冻结manifest SHA与重新计算的pair快照SHA共同锁checkpoint；恢复时重验源分片与内容，验证/test只用train拟合的IDF/权重评分。

最终隔离同次入口：`PYTHONPATH=training/src:training/search <锁定warehouse-runtime>/.venv/bin/python training/search/acceptance.py --runtime <锁定warehouse-runtime> --output <新证据目录>/run`。本次使用锁定runtime`/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-warehouse-20260914.l8dg0GbK2j`，退出0；证据`/private/tmp/sea-search-qrel-trainer.H2WUWL/run/report.json`，其`clickhouse/`、`seaweed/`、`build-v1/v2/`、`dataset-v1/v2/`与`candidate-v1/v2/`保留原Parquet、dbt测试、候选和checkpoint。此入口复用仓内数仓生产函数从两批显式**synthetic_fixture**源事件生成v1/v2，S3仍在线时由独立PyArrow S3 reader以生产者manifest SHA接纳，不把本地缓存行替代原对象。v1为13行、train/validation/test配对3/2/2，v2为12行、配对2/2/2；v1 train保留(3,0)/(3,1)，v2 validation也保留(3,1)。两代manifest SHA与pair快照SHA不同，旧v1 Parquet/manifest字节不变；中断后重新打开S3 reader并重验全部源对象，完整与恢复的checkpoint和候选字节逐字相等。

负例拒绝错误预期manifest SHA、大小变化与同大小Parquet篡改、未判断mask、来源冒名、近重复簇跨split、同query无高低判断对，以及pair内容/声明hash不一致后的恢复。`PYTHONPATH=training/search:training SEA_SEARCH_QREL_PRODUCER_EVIDENCE=/private/tmp/sea-search-qrel-integrated-20260915 uv run --project training --locked --python 3.12 python -m pytest -q training/search/tests training/tests`为**99 passed**。候选始终`status=candidate_only/active=false`，报告`model_quality=null`、`business_improvement=null`，loss仅为优化诊断；没有人工/observed qrel、在线效果、DC装载或Dense/Sparse/Multi-vector权重训练。CLI匿名本机S3只是隔离夹具，受认证共享S3、超大分片/多机恢复及正式模型评测另验。
