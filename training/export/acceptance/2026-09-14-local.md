# WS09-D 候选导出局部验收：2026-09-14

基线：BTW `68c4c6f`；实现提交 `4e6ced2`；分支 `feat/training-export-20260914`。工作范围仅 `training/export/`。H11.a 子合同版本 `sea.recommend.serving-candidate.v1`，输入训练 recipe `sea.recommend.cpu-towers-lr.v1`，H10.b row contract `sea.recommend-engagement.v1`，预处理 `sea.recommend.identity-f64.v1`。测试数据由 WS09-C 的 synthetic Parquet fixture 生成；**不是**实际 CH/S3 成熟用户行为。

复验命令（`training/` 目录）：

```sh
uv run --locked --python 3.12 python -m pytest -q export/tests recommend/tests tests
# 70 passed
cd search
uv run --project .. --locked --python 3.12 python -m pytest -q tests
# 11 passed
```

固定样例覆盖已完成训练和中断恢复后的 checkpoint；导出前全量复验输入 manifest/Parquet，要求 train 行 hash 和原始配置一致。对三组预设数值输入，训练双塔点积/LR logit 与重新装载推理包的独立算式误差均 ≤ `1e-12`；同 checkpoint 两次导出各文件字节相同。推理包不含 optimizer/RNG/reader 状态，`candidate`、`activation=none`、`registration=none`，重写现有目标目录被拒绝。CLI 实际输出两条结构化阶段日志和独立 stdout 结果。

反例：未完成 checkpoint、错误训练配置/输入 hash、变形或非有限权重、缺少/错序/错误预处理、训练器未消费的 `transform_refs`、不完整输入特征、非有限 serving 输入、文件 hash 篡改和 pair ID 配置错配均拒绝。实验性搜索词法候选只返回 `NOT_SUPPORTED`，没有 Dense/Sparse/Multi-vector serving 导出。

验收等级：推荐导出 `LOCAL_VERIFIED`；H11.a 整体 `PARTIAL`；H05 实际 DC 装载/探针 `NOT_VERIFIED`；H11.b 生效 `NOT_VERIFIED`；H12 仅局部对照与 CLI JSON 事件，Trace/指标/Collector `NOT_VERIFIED`。尚需 WS07-D/A 的真实冻结 CH/S3 输入、DC 提供推荐权重装载协议与实际探针、WS08-D/F 的用户/item 预构建和兼容接纳、WS09-E 的正式效果/实验评价。不能从 toy 对照推断正式模型可用或业务生效。
