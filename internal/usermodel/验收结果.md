# WS08-A 用户事实与运行投影验收结果

日期：2026-09-14。基线：`1684d66`，代码提交：`7a73931`，隔离分支 `feat/usermodel-facts-20260914`。范围：BreakTheWaves `internal/usermodel/`、`migrations/usermodel/`。生产者 RTW/DC、消费者 B/C/WS07 的真实进程未纳入本次包级验收。

工作区域：[W0] 本隔离 BreakTheWaves 工作树；[W1] 上述两个目录的事实、投影、迁移、测试和本文；[R1] Sea-Docs、RTW/DC 契约与 BTW 其他模块；[D1] 锁定 Go 模块缓存；[G1] 无；[X1] 仅本脚本临时 PG16 有测试写入，生产/共享服务未改；[N1] 其他工作树与目录；[T1] 随机临时 PG 数据目录。主职责 [C4] 事务持久化，交接 [C3] 语义状态、[C7] H08.b/H09 与 [C8] 验收。

| 局部要求 | 证据与结果 |
| --- | --- |
| 完整主体、并发幂等和冲突 | `TestPostgresReplayConflictIsolationAndAtomicOutbox`：12 路并发同 hash 仅一个事实/版本/outbox；异 hash 拒绝；同名主体跨 tenant 独立。**LOCAL_VERIFIED** |
| 乱序、修正、撤回、历史 | `TestPostgresOutOfOrderCorrectionRetractionAndHistory`：位置 3 先到呈缺口，1/2 到后连续水位到 3；修正/撤回只改变当前投影，5 条历史保留，分两页游标读取不重不漏。**LOCAL_VERIFIED** |
| 待关联与主体回补 | `TestPostgresPendingDependencyAndIdentityBackfill`：缺前驱事件待关联并自动回补；重投保持原收据；缺主体映射后绑定，外部别名跨事件不能串到另一用户。**LOCAL_VERIFIED** |
| 展示语义及归因 | `TestPostgresReadingDoesNotInventImpression`、`TestPostgresAttributionRequiresSameItemAndRequest`：阅读不伪造展示；实际展示、内容与请求一致才可归因；展示撤回后关联失效。**LOCAL_VERIFIED** |
| 同事务提交 | `TestPostgresOutboxFailureRollsBackFactAndVersion`：故意使 outbox 写入失败，事实/状态回滚。**LOCAL_VERIFIED** |
| OBS-01/02/03 的包级产出 | `TestPackageObservabilityWithInjectedProcessBundle`：注入共享 BTW Bundle 后，成功、待关联、冲突均输出开始/终态单行 JSON，分别标明 `succeeded/partial/rejected`，导出 3 个真实领域 Span；无 `fmt.Print*` 业务日志。**LOCAL_VERIFIED**；正式 Runner/Graph 与 Collector 的 OBS-05/07/08 **NOT_VERIFIED**。 |
| 顺序 | `TestReplayOrderHonorsSourceSequenceBeforeCrossSourceTime`：同源序列先于有时钟漂移的发生时间，跨源按可用事件时间选头。**LOCAL_VERIFIED** |

执行命令：`USERMODEL_KEEP_EVIDENCE=1 internal/usermodel/test-postgres.sh`，其中 `go test -race -count=1 -v ./internal/usermodel ./migrations/usermodel` 与 `go vet ./internal/usermodel ./migrations/usermodel` 均通过；`go test ./internal/... ./migrations/...` 也通过。保留的最近一次原始日志：`/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T//sea-usermodel-acceptance.3otrJP/go-test.log`；测试创建的 PostgreSQL 已停止，临时目录仅保留验收输出。

真实联调尚未验收：RTW 业务事件经 DC H04/H09.a 到 BTW 的来源、主体与证据对账；原始观察的语义接纳；用户 GraphAgent/typed Tool 的正式入口；Outbox 到 WS08-B/C 和数仓的可靠消费/回执；真实 Collector 下钻；非 1 来源起点的声明与验证。这些缺口使 **WS08-A 为局部完成，H08.b/H09 整体仍未 ACCEPTED**。
