# Wiki编制Worker应用候选与交接

状态：**本机可逆L1应用接口，正式DC作业/RTW对象与模型请求未接纳**。本仓独立分支从BTW内容集成`447b556446f01615de19dc3649cbdb19650d2b67`建立，只新增`internal/app/wiki_compile_worker*`；`internal/content/wiki_compile_{contract,graph}.go`原生编制合同、RTW/DC生成客户端、其它worker和原工件未改。产品/跨部门职责见Sea-Docs《WS02-C与WS06-A-Wiki编制执行交接》。

应用Interface为`WikiCompileWorker.ProcessClaim`与`RunOnce`，内部一次处理当前DC job attempt。它固定RTW/DC提供方的`content.wiki-compile.v1`/`cpu`/`ridethewind.knowledge`，先核`Job.InputHash=整8字段Submit的JCS/SHA`、单版本十二键`rtw.wiki.compile-ticket.v1`、规范十进制generation/cancel、当前worker/attempt/lease/deadline及原`source_event_id`/hash形状。DC技术输入哈希与RTW `Compile.InputHash`是**不同域**；`source_event_jcs_sha256`因RTW私有GetCompile当前不回Outbox原EventSpec，仅可核形状，**不宣称独立原事件字节回读**。随后`GetCompile`核module/page/base/有序SourceRevisionID集、原Guidance UTF-8 SHA、RTW业务input_hash/generation/cancel；每份`GetRevision`必须同module/source/ID、未撤回、原UTF-8正文hash=`ContentHash`及`ObjectKey=sha256/<hash>`。坏票据/原文在RTW Claim前拒。

DC当前job仍同attempt/epoch/cancel后才向RTW `ClaimCompile`送完全相同的lease栅栏。cmd权威装配从同一个`CurrentNativeSession`一次冻结私有`WikiCompileModelSession{AccountID,NativeBearer,TokenDigest}`传入应用；`WikiCompileRunFactory.Open(frozen WikiCompileInput,frozen ModelSession)`为**每逻辑Compile**据此造已授权`tRPC-Agent-Go model.Model`/现成Graph/Runner与私有Session，Run的`ModelSessionProof{AccountID,TokenDigest}`必须在`Runner.Run`**前后**同冻结值相等。另一合法DC账号B的Factory即使能构造Graph也在模型HTTP前拒，后续对象Put/RTW Accept/DC Complete为零；RTW Claim此前可能已发生，不假报零Claim。若Factory在Run后变proof，已发生的模型调用不能撤销，但应用仍阻候选对象/业务接纳/技术ACK；生产Factory须只用冻结Bearer构造官方Adapter，不能自报proofA却送bearerB。Bearer不进入job/Graph state、日志或指标；同账号令牌换代须以新worker实例再核。应用自己消费Graph completion及Runner完整EOF、终态错误、模型JSON/原来源`paragraph:N`和ticket锚；应用不硬编码某个模型或使用DC jobs service token调用模型Gateway。当前DC登记Chat CallPoint `knowledge-wiki-compiler`却无有效模型配置；模型适配由单独owner提供DC native app_user Bearer与每逻辑Compile稳定幂等Ref，**正式RunFactory以冻结会话直接构造该官方Adapter仍未在本分支装配**，所以只签接口L1，不以Factory自报proof冒称DC真实模型账号。应用用技术Session内部键分隔Compile/attempt，**不签外部RTW用户SubjectRef**。

同页新编制替代旧`BUILDING`时，RTW业务事务把旧Compile标`SUPERSEDED`、业务CancelVersion+1，RTW Jobs Sender向DC旧Job发送**取消请求**。如果旧Job正在运行，DC写`cancel_requested`且技术CancelVersion由原值加一；**终态不由RTW Sender代做ACK**。本Worker在每次当前Job读取（包括模型EOF后、候选对象Put前）仅当原Job/Submit/InputHash、worker/AttemptID/Attempt序号/lease epoch均不变、旧租约尚有效、状态确为`cancel_requested`且CV精确原值+1时，调用官方`AcknowledgeCancellation`用**新CV**确认取消。丢HTTP回执先`GetJob`核同一Job/attempt/version已`cancelled`，不能拿成功`CompleteJob`代替；仍`cancel_requested`时记`DC_CANCEL_TERMINAL_PENDING`，身份/版本/租约错则零ACK。DC的`Get/Claim`也会在取消租约到期时转`cancelled`，官方接口对已被DC租约过期自动转成`cancelled`的同lease仍可返回200；当前没有“ACK applied”收据，即使本Worker发过ACK请求，也只能确认同Job/attempt/CV的取消终态，不能归因终态由Worker施作。RTW旧Compile已`SUPERSEDED`而DC取消请求尚未到达时保持`Fence`、零模型/对象/RTW Accept/成功Complete；确认取消终态只收束DC技术作业，**不接纳旧Wiki候选**，OBS `DC_CANCELLED_CONFIRMED`/`dc_cancel_terminal=true`和`rtw_accepted=false`分别记录。

Runner通过后再次核RTW/DC lease与取消，再将候选Markdown**原字节**写内容寻址`artifacts.Store`并同Store Get逐字节读回；当且仅当RTW接纳与该Store共享同根或同桶，RTW `AcceptCompile`才能自己用其`ObjectStore.Get(key,hash)`证明原字节。再次核当前job/compile后送`ObjectKey,ContentHash,Title,SourceRefs`，只认RTW `State=ACCEPTED`、原Compile/attempt/epoch/cancel及非空RevisionID/ResultHash；`ResultHash`是RTW对**整个Accept请求**的摘要，不能当Markdown内容SHA。未知RTW HTTP回复先`GetCompile`核未变栅栏，再用**完全同一Accept请求**重投让RTW自身核同体ResultHash；错栅栏/取消/同键异文不自动恢复，也不覆盖人工Wiki修订。

DC `CompleteJob`只能在RTW业务接纳之后。提供方明确尚无跨仓成功`ResultRef{URI,Hash,MediaType}`冻结合同，构造器的`CompletionRef=nil`为默认：RTW已接纳时返回`ErrWikiCompileTechnicalPending`和结构化`partial/rtw_accepted=true/dc_completed=false`，**不会调用DC Complete/ACK**。本机仅通过显式fixture `CompletionRef`验证顺序及丢RTW/丢DC回复的`GetCompile`/`GetJob`同键恢复；它所用`fixture://`URI只是测试私有值，不能交正式任务。若进程在RTW已接纳后重启却失去原候选/Accept请求，本模块不伪造结果Ref或技术ACK，交后续持久候选收据/RTW可回读结果合同owner设计。

## 本机证据与等级

`TestWikiCompileWorkerNativeAcceptedBeforeOptionalTechnicalACK`在隔离子进程使用**真正**框架Runner/Graph/LLMAgent、固定模型、同一个任务专属Local对象Store给BTW写和RTW权威fixture读。测试先签默认nil CompletionRef的RTW Accepted/零DC成功Complete，再签explicit fixture Ref的RTW未知回复→GetCompile/同体Accept及DC未知回复→GetJob状态与结果Ref核对；DC在模型EOF后发`cancel_requested`，fixture经本Worker的ACK请求达`cancelled`，候选对象Put/RTW Accept/成功Complete仍零；RTW同页新代将旧Compile标SUPERSEDED但DC取消尚pending则只Fence不ACK；模型伪造locator、技术Submit哈希坏行同样阻断。原生`workflow execute_graph wiki_compile_candidate`的`trpc.agent.go` Scope与应用`content.wiki_compile.process`在同一Trace，JSON日志把技术`DC_CANCELLED_CONFIRMED`与业务`rtw_accepted=false`分清，不以fmt直接输出阶段或把源码正文/模型口令写日志。

本机聚焦`GOMAXPROCS=2 go test -mod=readonly -race -p=1 -count=1 -run '^TestWikiCompileWorker' ./internal/app`退出0，最新日志SHA=`528c75a06e83708743c80445c184ee254f76d170c0ac80bd98864354f673095d`：直接缺冻结会话构造拒，A/B模型会话错绑时RTW Claim已发生1但模型调用0/对象Put0/RTW Accept0/DC Complete0；Run后proof变B时模型fixture调用1但所有后续写仍零。技术取消正反例核同claim的ACK、丢回执Get终态恢复、409仍pending、别的Job/身份/attempt/epoch/Submit/version和过期租约零ACK。`go vet -mod=readonly ./internal/app ./cmd/worker`退出0。提供方真RTW×DC隔离PG16冻结的无令牌票据JSONL内容SHA=`04d33b7f5e242b80c791c3697bcc8d5a8ac527ff130254607c16222872481aec`，本仓**只读**在其原Claim lease窗口重播十二键/八字段/完整Submit JCS且race同轮通过；它不是当前在线租约证明，也没有重跑该跨仓PG。本机可用盘约11Gi时暂停大PG/CH/race套验，保留已有证据与停止实例。

取消ACK另有小型**真实DC-only**本机隔离PG16/HTTP验收：`SEA_WIKI_DC_ROOT=<固定DC树> bash internal/app/wiki_compile_cancel_dc_acceptance.sh`只建一份DC binary，不启动RTW、模型或对象服务。官方BTW Client Submit/Claim→真实DC Cancel running返回`cancel_requested`/CV+1；错误worker请求零ACK保持原态；相同旧attempt/epoch/worker带新CV经应用栅栏和官方`/cancel/ack`变`cancelled`，再次读取不发第二逻辑ACK。第二份2秒真实租约先由DC `GetJob`懒转`cancelled`，随后同lease `AcknowledgeCancellation`仍HTTP200且`UpdatedAt`不变；确定性fixture又覆盖Worker初读`cancel_requested`、ACK锁前DC自动到期的竞态，公开OBS均只签终态确证。聚焦race退出0，test.log SHA=`39287dc4576b0ec3a8b696f63b563babc43dce1e767e70ccd3d7ee8eb0bb5c06`，固定DC源码HEAD=`0e6f0fe0791807538ce317ca18ecb8747d0f615f`、其SHA文件摘要`49b8fccd6c09effd409117d18a89007aa06c630fc016ad9679cc768f87ba4ed9`，PG停机`status3`/stop.log SHA=`ca19178a35ab4153b75b494963b66ce8243e1b94c173107c87b44d09db23652d`；证据目录留存`/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-wiki-cancel-acceptance.BCjZHP`。这签DC技术取消收尾，不签RTW同父原Wiki候选/成功ResultRef。

签收仅**应用Interface/来源拒错/原生Runner与对象同Store夹具/业务先于技术提交的L1**。正式RTW Outbox原事件字节私有读、RTW/DC真同轮Claim→实际Wiki候选原字节、DC成功ResultRef合同及持续恢复账本、正式S3同桶权限/生产对象读回、DC native模型app_user会话/有效配置/真实Provider请求和`cmd/worker`默认关闭装配门禁均欠签。RTW接纳WikiRevision也不自动建立Release/发布或改人工修订。WS06-A及47项仍`PARTIAL`。
