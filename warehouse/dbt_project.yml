name: sea_warehouse
version: '0.1.0'
config-version: 2
profile: sea_warehouse
model-paths: [models]
test-paths: [tests]
macro-paths: [macros]
clean-targets: [target, logs]
models:
  sea_warehouse:
    +materialized: table
    +engine: MergeTree()
    +order_by: tuple()
vars:
  label_window_seconds: 1800
  label_rule_version: effective-read-fixture-v1
  feature_contract_id: recommendation-engagement-fixture-v1
