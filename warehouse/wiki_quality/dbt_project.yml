name: sea_wiki_quality_source_warehouse
version: '0.1.0'
config-version: 2
profile: sea_warehouse
model-paths: [models]
test-paths: [tests]
clean-targets: [target, logs]
models:
  sea_wiki_quality_source_warehouse:
    +materialized: table
    +engine: MergeTree()
    +order_by: tuple()
