version: 2
sources:
  - name: favorite_landing
    schema: "{{ var('favorite_landing_schema') }}"
    tables:
      - name: ods_favorite_event
        description: Independently ACKed DC source, verified against RTW immutable EventSpec and technical receipt.
      - name: ods_favorite_event_subjectref_v2_r1
        description: Historic-v1 row plus issuer and canonical UID, exported only from a verified PG sidecar and archived under a new ODS revision.
