name: sea_user_feature_baselines
version: '0.1.0'
config-version: 2
profile: sea_warehouse
model-paths: [models]
macro-paths: [macros]
models:
  sea_user_feature_baselines:
    +materialized: table
    +engine: MergeTree()
    +order_by: tuple()
