# WS07-C → WS08-C 用户特征基线交接验收

日期：2026-09-14。状态：**L2 隔离跨存储联验通过；WS07-C/H10.c 整体仍为 PARTIAL**。

| 验收项 | 实际证据 |
| --- | --- |
| 来源与水位 | 测试在真实隔离 PostgreSQL 中通过 usermodel `Append` 接纳序号 1、3；第一代 PG `REPEATABLE READ` 快照给出连续前缀 1，仅一条完整可逆贡献入 DWS，序号 3 保留尾部；补入序号 2 后第二代前缀推进至 3。序号 4 虽已接纳但发生时间在未来，未被提前纳入基线。 |
| 数仓 SQL | SeaweedFS S3 固定 JSONL 经 ClickHouse `s3()` 入原始表；dbt 单模型 `dws_user_feature_values` 在两代独立 ClickHouse schema 中成功执行。SQL 计算同版 `FeatureSpec` 的 count/latest、窗口、默认值/OOV。 |
| 工件 | 两代分别保留 S3 来源、DWS 值、完整 `FeatureBaseline`、dbt `manifest.json`、`run_results.json` 和含 SHA-256 的交接清单；真实目录见下。 |
| 消费与拒收 | S3 下载、哈希和贡献数核对后，`AcceptFeatureBaseline` 对照 PG ledger 接纳；第一代含未覆盖尾部而总值为 2，第二代值为 3 且无当前尾部。重复接纳、旧代回退、篡改清单或对象 hash 被验证；不可变 `feature_snapshot_id` 可回读。 |
| 未来事实 | 第二代在序号 4 未生效时仍只覆盖到 3；快照 `NextChangeAt` 指向序号 4 的业务生效时刻，防止未来持续读取过期当前值。 |

最终命令 `python3 warehouse/feature_baselines/acceptance.py --runtime /tmp/sea-ws07c-ws08c-runtime-verified-20260914` 退出码 0；Go 测试使用 `-race -count=1`。日志与隔离 PG/ClickHouse/SeaweedFS 数据留在 `/private/tmp/sea-ws07c-ws08c-runtime-verified-20260914/feature_handoff_zymvurms/`，进程已停止。`go-test.log` 给出两代 S3 来源、DWS 值、基线和 dbt manifest 的实际 SHA-256。既有 warehouse Python 9 项测试、Go vet 与 `go mod verify` 通过。

`USERMODEL_KEEP_EVIDENCE=1 bash internal/usermodel/test-postgres.sh` 退出码 0，涵盖新增未来事实失效用例以及 `./internal/usermodel`、`./migrations/usermodel` 的真实隔离 PG/race 和 vet；最终日志保留在 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-usermodel-acceptance.xzI17n/go-test.log`。该回归与跨存储测试使用不同隔离 PG 实例。

这是**合成业务事件驱动的真实 PG/ClickHouse/S3/PG 存储联验**，不是“真实用户流量”或生产发布。现有通用 `recommendation` DWS 仍不等同于 usermodel 语义事实 DWS；本切片另建独立 dbt 项目。正式 H09 来源回执、多分区来源完整性、DC 分布式任务 lease/Outbox、正式批准的 FeatureSpec、对象存储并发 CAS、用户塔/推荐请求消费和完整 WS07-C 其他数据产品均未验收。`Outbox.created_at` 不是 PG commit timestamp，严格历史请求要保存不可变 `feature_snapshot_id`。

环境引导原来在安装依赖之前就 import `jsonschema`，且 ClickHouse 25.8.28.1 macOS ARM64 的锁定 SHA 与官方 release API 的 `sha256:5f14f26104eb3c09f042e5c899529d25cef4f174507e9e73a3346325d0081429` 不符；已修正并用实际下载文件核对后才启动服务。官方资产信息见 [ClickHouse release API](https://api.github.com/repos/ClickHouse/ClickHouse/releases/tags/v25.8.28.1-lts)。
