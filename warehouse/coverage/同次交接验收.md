# WS07→WS08→H10 同次覆盖交接验收

本脚本从同一个父运行中启动隔离 RTW Favorite、DataCenter platform、PG16、ClickHouse/dbt 和 SeaweedFS。测试只写 `warehouse/coverage/`；WS07 Publisher、WS08 Verifier、H10 Builder 的实现与 Sea-Docs 保持原样。执行：

```sh
SEA_DC_PLATFORM_ROOT=/path/to/isolated/datacenter \
SEA_RTW_FAVORITE_ROOT=/path/to/isolated/rtw \
WAREHOUSE_FAVORITE_RUNTIME=/path/to/locked/warehouse-runtime \
warehouse/coverage/combined_acceptance.sh
```

`combined_test.go` 的**真 RTW/DC**路径：旧收藏领域操作产生 assert/retract、DC 持久给 offset 1/2；WS07 自己的 consumer 先接纳 ODS、批次证据并发布 W1 原始 S3 EventIndex/BatchEvidence/SparseIndex。WS08 以另一个 PG 数据库中的真实 tRPC-Agent-Go FactGraphRuntime/Store/Outbox 接纳同源事实，`FavoriteCoverageHTTPProof` 向真实 RTW/DC 重查后调用 `VerifyPrefix/VerifySubject`。H10 `UsermodelCoveredReader` 与 `RunnerCoveredObjects` 读取同一 S3 对象，`BuildCovered` 得到默认关闭的候选。随后撤回进入领域 PG，历史 W1 仍为建立值 1，尾部显示撤回且 `CurrentComplete=false`；W2 候选才为 0。两边 DC consumer 的 ACK 各自推进，WS08 验证本身不 ACK。

第二条路径使用**第二个真实隔离 DC/PG**，RTW 业务写入与私有权威读由明确的测试 HTTP 夹具代替，因此不能称真实 RTW 多用户业务来源。夹具给 DC 按 `[u1 assert:1,u2 assert:2,u1 retract:3]` 发布同一 producer 的冻结 EventSpec。WS07 实际发布 W1/W3 和 `[1,3]`、`[2]`、空主体三份稀疏切片；Graph 在 offset 2 尚未接纳时先把 offset 3 通过 RTW 校验后的真实 Graph/Store/Outbox 提交，验证 W1 **候选值**仍为 1、已知尾部 3 和 `CurrentComplete=false`，W3 的 `VerifyPrefix` 只能返回 Pending。补入 offset 2 并重投 offset 3 后，W3 验根成功，H10 对 u1/u2/空主体分别得到 0/1/0。旧 W1 EventIndex 与候选 hash 保留。

拒错门禁使用**同次 Publisher 产生的原字节**：省去全局 offset 2、RTW 权威主体与索引主体冲突、少领域 PG 事实、篡改 WS08 SparseIndex 输入、篡改 H10 真 S3 SparseIndex 对象，均不得变为可用候选。ClickHouse/dbt 在相同父运行里从这两条 PG ODS 导出生成独立收藏 DWD，核主体、offset 和正反状态；收藏不进入推荐曝光/成熟标签。`cross-domain/report.json` 分列真源和双主体夹具的证据等级；`cross-domain/artifacts/` 保留逐 SHA 验证的 S3 源字节与候选工件，localhost URL 仅在本次运行期间可访问。

当前父验收已从 **WS07 发布、WS08 覆盖接纳和 H10 默认关闭候选**延伸到 WS08-C 的`AcceptCoveredBaseline`历史收据。它仍没有 FeatureSnapshot/ServingBundle 激活，也没有正式获批 FeatureSpec、真实 RTW 双用户流量、DC 用户塔或推荐效果证明。`CurrentComplete` 只表示该 PG 快照没有已知尾部；DC 全局鲜度和线上放行另有门禁。

此前未含v2历史接纳的连续门禁于 2026-09-15 退出 0，证据目录为 `/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-coverage-combined.6FBUzq`。其ODS/DWD、根/切片和候选SHA仍可按原目录回查；最新含v2接纳的同次证据见下节。

## H10原字节到WS08-C历史接纳

父测试为两个隔离model PG显式依次执行`001_facts.sql`→`002_coverage_verification.sql`→`003_features.sql`→`004_serving.sql`→`005_covered_baseline.sql`，初始、每笔接纳后及末尾均核v1特征基线/头、FeatureSnapshot/版本、ServingBundle/指针六表为空。每笔先从同次H10候选的SeaweedFS URL按SHA-256读回**原始**`CandidateJSON`，核其与H10对象完全同字节，再交`Store.AcceptCoveredBaseline(CoveredBaselineSubmission{ArtifactURL,ArtifactSHA256,CandidateJSON,FeatureSpec})`。新收据恒`accepted_historical_default_off`、同原字节重放为`Replay=true`；PG`candidate_raw bytea`与S3原字节逐字节一致。旧W1修订异哈希返回Conflict，损坏原字节沿用旧哈希返回Invalid；H10的源/稀疏对象篡改仍在前段拒收。

真RTW/DC单主体在撤回进入PG后把W1历史值1、Tail offset2、`CurrentComplete=false`的候选作为修订1接纳，W2值0作为修订2接纳。双主体隔离权威HTTP夹具在offset3撤回已接纳、offset2尚未接纳时，把u1 W1值1和尾部offset3作为修订1接纳，状态仍default-off；补齐2并验W3后，u1/u2/空u3分别以值0/1/0得到修订2/1/1历史收据。W3覆盖与各主体切片仍来自原WS07全局索引，两个DC consumer/ACK各自推进，v1和Serving头始终为0。

最终代码在BTW基线`3b6588d`的独立分支运行`SEA_DC_PLATFORM_ROOT=<DC集成树> SEA_RTW_FAVORITE_ROOT=<RTW集成树> WAREHOUSE_FAVORITE_RUNTIME=<锁定运行时> bash warehouse/coverage/combined_acceptance.sh`，2026-09-15退出0；证据目录`/var/folders/f_/l5hv3b1d6sx8zwr_cc8fkjkm0000gn/T/sea-coverage-combined.EvdkUf/`。`cross-domain/report.json`是父脚本核两份Go历史收据后的**最终**摘要，`accept_v2=accepted_historical_default_off`、`activation=default_off`；`real-joined-ref.json`留2笔、`two-joined-ref.json`留4笔收据，`artifacts/{real,two}/candidates/`留H10原始候选字节。`cross-domain.log`保留Python在v2接纳摘要合并前的中间报告，不能拿其中旧`not_called`字段替代最终`report.json`。RTW源race、两个BTW Go race、PG16/ClickHouse/dbt/SeaweedFS、vet/mod verify同次退出0。证据等级仍分别为真RTW/DC单主体与真实DC＋RTW权威HTTP夹具双主体；批准FeatureSpec、真正可服务v2 Snapshot/Serving、真实RTW双用户流量和线上运行均未验。
